# METER/ — legacy input directory

This folder contains data that pre-dates the current directory layout.

**New location for Zentra Cloud exports:** `data/usr/inputs/Zentra/`

**New location for user-maintained info files:**
- `data/usr/inputs/z6_info_wide.csv`
- `data/usr/inputs/serials.txt`

The `z6_info_wide.csv` and `z6_info_long.csv` copies inside this folder are
kept for reference only and are not read by the current scripts.
